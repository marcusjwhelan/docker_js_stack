apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pv-claim
spec:
  storageClassName: ""
  volumeName: mysql-pv-volume
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi